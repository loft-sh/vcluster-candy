# vcluster-candy end-to-end tests

This suite verifies that DNS queries from tenant pods are routed to the correct resolver.

## Requirements

`docker`, `kind`, `helm`, and a Go toolchain matching the module's `go` directive.
